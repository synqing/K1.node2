Artifacts produced by automated tests and tooling are placed here.

- Playwright JSON: `playwright-report.json`
- Vitest (json via reporter): accessible via CLI; dev server serves `/artifacts/*.json` if present